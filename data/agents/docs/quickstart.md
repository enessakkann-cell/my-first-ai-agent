# Quickstart Guide

1. Open the `data` folder and upload your latest production data in `.csv` format.  
2. The n8n workflow will detect the new file.  
3. The AI agent reads the file and asks Claude/OpenAI to summarize it.  
4. The generated report is saved under `/docs/weekly_report.md`.  
5. Review the report and share it with management.
